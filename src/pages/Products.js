import React, { Fragment } from "react";
import HeaderProducts from "../component/Header/HeaderProducts";
const Products = (props) => {
    return (
        <div className="container">
            <HeaderProducts />
        </div>
    );
}
 
export default Products;